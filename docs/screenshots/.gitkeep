# Demo screenshots

Place annotated BioDex output screenshots here for the README and presentations.

Suggested captures:
- Field Review UI: batch stats panel + frames table after Process Folder
- Side-by-side original vs annotated frame from the review panel
- Optional: collapsed Single-image spot check (secondary)
- Terminal: batch_report.txt from `biodex batch` (power-user workflow)

See [docs/demo.md](demo.md) for step-by-step instructions.
