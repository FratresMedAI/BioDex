# Desktop packaging (work in progress)

PyInstaller specs in this folder are **not ready for distribution**.

For real use today, install from source and run locally:

```bash
bash scripts/install_biodex.sh
biodex-ui
```

See the main [README](../README.md). Desktop `.exe` / `.app` bundling is planned future work.
